export const setupLoadBaseDependencies = async () => {
    window.loadBaseDependencies = async () => {
        // Use this function to extend the configuration for example on backend related stuff
    }
}